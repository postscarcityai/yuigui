// The live release demo's entry (YUI-90), without its lines. The playground runs in the browser, so it
// takes this and gets the lines from its server page (release-sample.mjs), not the board exports themselves.
export const RELEASE_META = [
  { slug: "release", name: "War room: the release timeline (live from the board)", agent: "Yui", yl: "" },
];
